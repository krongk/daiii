
<%= link_to image_tag(asset_path('logo.png')), root_path, :class => 'brand' %>
<ul class="nav">
  <% if user_signed_in? %>
    <li>
      <%= link_to "添加分类", new_site_cate_path, data: { toggle: "modal", target: "#ajax-modal" } %>
    </li>
    <li>
      <%= link_to "添加网址", new_site_item_path, data: { toggle: "modal", target: "#ajax-modal" } %>
    </li>

    <li>
    <%= link_to '退出', destroy_user_session_path, :method=>'delete' %>
    </li>
  <% else %>
    <li>
    <%= link_to "登录", "/home/modal_window", data: { toggle: "modal", target: "#ajax-modal" } %>
    </li>
  <% end %>
  <% if user_signed_in? %>
    <li>
      <%= link_to '管理账户', edit_user_registration_path %>
    </li>
    <% if current_user.has_role? :admin %>
      <li>
      <%= link_to '后台', users_path %>
      </li>
    <% end %>
  <% else %>
    <li>
    <%= link_to '注册', new_user_registration_path %>
    </li>
  <% end %>

</ul>

<%= render 'layouts/modal' %>